// App.tsx is the root component — everything starts here.
// For now it just renders the Terminal. Later we'll add a sidebar and layout.

import Terminal from "./components/Terminal/Terminal";

function App() {
  return <Terminal />;
}

export default App;
